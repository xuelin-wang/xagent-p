apiVersion: v1
kind: ConfigMap
metadata:
  name: langchain-service-config
data:
  OPENAI_MODEL: gpt-4.1-mini
  OPENAI_EMBEDDING_MODEL: text-embedding-3-small
  XAGENT_MAX_WAIT_SECONDS: "10"
  PORT: "8000"
