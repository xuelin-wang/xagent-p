apiVersion: v1
kind: Service
metadata:
  name: {{ include "langchain-service.fullname" . }}
  labels:
    {{- include "langchain-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "langchain-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
